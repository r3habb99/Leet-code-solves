    const answer = new Array(n);        // Step 1: Compute prefix products    // answer[i] contains the product of all elements to the left of i    answer[0] = 1;    for (let i = 1; i < n; i++) {        answer[i] = answer[i - 1] * nums[i - 1];    }        // Step 2: Compute suffix products on the fly and combine    let rightProduct = 1;    for (let i = n - 1; i >= 0; i--) {    const n = nums.length;var productExceptSelf = function(nums) { */ * @return {number[]} * @param {number[]} nums/**